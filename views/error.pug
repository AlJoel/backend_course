extends layout

block content
    .page-header
        h1 Error de acceso

    .form-card
        p= mensaje || 'No tienes permisos para ver esta página.'

        .form-actions
            a.btn.btn-secondary(href='/') Ir al menú principal
