extends ../layout

block content
  h1= titulo

  form(action="/empleados/agregar" method="post")
    label(for="id") ID
    input#id(type="number" name="id" required)
    br

    label(for="nombre") Nombre
    input#nombre(type="text" name="nombre" required)
    br

    label(for="dni") DNI
    input#dni(type="text" name="dni" required)
    br

    label(for="rol") Rol
    input#rol(type="text" name="rol")
    br

    label(for="area") Área
    input#area(type="text" name="area")
    br

    button(type="submit") Guardar
